---
title: "更新日志"
description: "ag-vibe 各项目的版本更新记录。"
icon: "clock-rotate-left"
rss: true
---

<Update label="2026-03-30" description="Haklex 编辑器接入、memo 数据结构重构，以及 todo / starter / docs 同步推进" tags={["AllinOne", "memos", "todo", "starter", "docs"]}>

## AllinOne

- memo 服务改为以结构化编辑器 JSON 持久化 `content`，并在响应中直接返回 `plainText`、`excerpt`、`tags`、`references`
- 对齐 memo 相关 OpenAPI、SQL 查询、迁移与 handler / service / parser 测试，减少前后端各自派生字段的不一致
- 补充 todo 描述与 bucket 管理测试，并更新开发技能与依赖配置

## memos

- 编辑器切换到 Haklex，前端开始直接编辑和渲染结构化内容，为后续自定义块和富内容扩展打基础
- memo 编辑器增加可见 Toolbar，支持段落、标题、引用、粗斜体、下划线、行内代码、列表、分隔线，并把输入区拉高后再压紧 `article` 样式的 padding
- 补齐登录 / SSR / hydration 处理、memos E2E 测试框架，以及标签过滤相关测试

## todo

- 持续修正 Cloudflare Pages / Nitro / PWA 发布链路，避免 HTML 被旧缓存卡住
- 登录前置鉴权与 hydration 处理加强，SSR 下的认证判断更稳定
- 清理 `TodoWithDescription` 和 `todo-descriptions` 旧结构，统一为 `TodoItem`

## starter

- `starter` 仓库初始化完成，补上 API base URL helper 测试并接入 Vitest 配置
- CI 默认 `workdir` 设置简化

## docs

- 新增 memos 项目文档页面
- 更新文档结构与更新日志页

</Update>

<Update label="2026-03-29" description="AllinOne memo 服务、auth SDK、ido 交互与 memos v1 首发" tags={["AllinOne", "auth", "ido", "memos"]}>

## AllinOne

- 新增 memo 服务，支持 CRUD、标签和 backlink 关系
- 补齐附件管理能力，新增附件与附件关联接口
- 扩展 OpenAPI 与数据库迁移，带动后端 API 与生成代码同步更新

## @ag-vibe/auth

- `@ag-vibe/auth` 升级到 v0.1.3，`baseUrl` 支持传入函数，适配运行时可变的 API 地址
- 修正 `InterceptableClient` 中 `signal` 类型兼容性，提升与不同运行时的适配稳定性
- `createAuthClient` 支持自定义认证端点路径，方便接入不同后端约定
- 新增 `waitForHydration`，帮助前端在会话恢复完成后再进入主流程
- 新增 GitHub Actions CI / CD workflow，CD 使用 npm Trusted Publishing（OIDC），无需长期 token
- 开发者文档正式上线，涵盖快速开始、令牌刷新、会话 store、401 拦截器

## ido

- 接入 `@ag-vibe/auth` v0.1.3，并配合 `waitForHydration` 优化会话初始化
- 支持双击创建任务，并允许在任意列直接创建任务
- 更新应用图标与品牌资源
- 重整认证和主题相关逻辑，提升首屏与同步体验

## memos

- `memos` v1 正式上线，完成首页到内容页的整套产品首发
- 新增认证页、应用壳、memo 列表、编辑器、活动图谱与内容卡片
- 重新整理首页路由与样式，形成可直接使用的 memos 工作台

</Update>

<Update label="2026-03-28" description="编辑体验与 API 规范稳定性更新" tags={["ido", "AllinOne", "auth"]}>

## @ag-vibe/auth

`@ag-vibe/auth` v0.1.0 首发，作为 ag-vibe 生态的前端认证 SDK 正式可用。

- 基于 Zustand 的会话 store，支持 `localStorage` 持久化与跨标签页同步
- 访问令牌过期前主动刷新，默认 60 秒提前量
- 401 自动重试拦截器，兼容 `@hey-api/client-ofetch` 生成的客户端
- 并发刷新去重，避免多个 401 触发多次 refresh 请求
- 刷新失败容错：网络错误、429、5xx 不清除会话，仅 401/403 登出
- 非 DOM 环境自动降级为内存存储（SSR、Worker）

```bash
pnpm add @ag-vibe/auth
```

## ido

- 任务编辑体验升级：标题编辑后支持 `Tab` 直接进入描述编辑
- 任务卡支持展示描述，空描述不再显示占位内容
- 描述增加本地持久化兜底，弱网下更不容易丢失
- 仅在生产环境展示 SW 更新提示，开发环境自动清理 SW 与缓存

## AllinOne

- 修复 OpenAPI 版本字段格式，`openapi` 明确为字符串 `"3.0.0"`
- 提升 OpenAPI 文档导入、校验与 SDK 生成链路兼容性

</Update>

<Update label="2026-03-24" description="部署链路增强与发布兼容性改进" tags={["ido", "AllinOne"]}>

## ido

- 部署流程切换并对齐 Cloudflare Pages 发布链路
- 修复 wrangler 版本与参数兼容问题，提升构建稳定性
- 清理多余部署步骤，降低 CI 发布失败率

## AllinOne

- 增加生产 `Dockerfile` 与 `docker-compose.prod.yaml`
- 新增 GitHub Actions 部署流程，支持构建并推送 GHCR 镜像
- 增加远程服务器自动拉取并重启服务的发布步骤

</Update>

<Update label="2026-03-22" description="ido 与 AllinOne 核心能力首发" tags={["ido", "AllinOne"]}>

## ido

- 看板核心能力上线：`Later / This Week / Today / Done` 四栏与拖拽整理
- 支持任务新增、重命名、删除、完成标记等完整 CRUD 交互
- 认证流程上线：登录/注册、Token 存储、自动续期与失效回收
- 离线同步闭环上线：离线操作入队，恢复网络后自动回放
- 首版 PWA 与深色模式落地，支持安装与更新提示

## AllinOne

- 后端项目初始化完成，提供可运行的开发环境与基础 API
- TODO API 首发：`/todos` 列表/创建与 `/todos/{id}` 更新/删除
- 数据模型与迁移上线，支持用户与任务数据持久化
- 引入任务分桶能力（`later/week/today`）及归一化处理
- 新增 TODO 子集 OpenAPI 生成能力与 BearerAuth 鉴权声明

</Update>
